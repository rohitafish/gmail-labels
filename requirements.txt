# Exact pins (==), not floors (>=): an unpinned dependency lets a fresh
# `pip install` resolve something the current .venv never actually ran,
# which is exactly the kind of drift the test suite and CI can't catch
# because they never see it. Bump these deliberately, in their own commit,
# when there's a reason to.
google-api-python-client==2.199.0
google-auth-oauthlib==1.4.1
